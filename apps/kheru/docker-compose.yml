name: kheru

services:
  gentle:
    image: lowerquality/gentle
    container_name: kheru-gentle
    ports:
      - "8765:8765"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8765/"]
      interval: 5s
      timeout: 5s
      retries: 24
      start_period: 30s
    restart: unless-stopped

  app:
    build:
      context: ../..
      dockerfile: apps/kheru/Dockerfile
    image: kheru-app
    container_name: kheru-app
    ports:
      - "3000:3000"
    environment:
      HOST: 0.0.0.0
      PORT: 3000
      DATA_DIR: /data
      TRANSFORMERS_CACHE: /data/transformers-cache
      GENTLE_URL: http://gentle:8765
      GENTLE_WAIT_SECS: 180
    volumes:
      - ./data:/data
    depends_on:
      gentle:
        condition: service_healthy
    restart: unless-stopped
